# Optional: full API URL for production builds (no Vite proxy).
# Leave unset in dev to use Vite proxy to http://127.0.0.1:8000
# VITE_API_BASE_URL=http://127.0.0.1:8000

# Optional: paste values from notebook.ipynb AFTER you run a real hold-out evaluation.
# The API does not return test accuracy — the UI shows these only if set (no invented metrics).
# VITE_ML_HOLDOUT_ACCURACY=0.87
# VITE_LLM_HOLDOUT_ACCURACY=0.91
# VITE_ML_HOLDOUT_F1=0.9125
# VITE_EMBEDDING_DIMS=384
